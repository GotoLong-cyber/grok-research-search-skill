# 安全说明

1. 不要把真实 API key 提交到 GitHub。
2. 不要提交 `.env`。
3. 如果 key 泄露，立即去中转站后台重置。
4. 检索结果必须人工核验。
5. 第三方中转站是否支持实时检索取决于服务商。

补充提醒：

- 不要把包含真实 `GROK_API_KEY`、真实 `GROK_BASE_URL`、个人代理地址、个人 shell 配置的文件放进仓库。
- 如果要分享给他人，只分享 `.env.example`，不要分享你自己的 `.env`。
